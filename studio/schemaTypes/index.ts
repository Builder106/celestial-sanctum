// Schemas live one level up at ../sanity-schemas so they're visible to both
// the Studio (this folder) and the Angular type system. Don't duplicate them
// here — edit ../../sanity-schemas/*.ts instead.
export { schemaTypes } from '../../sanity-schemas';
